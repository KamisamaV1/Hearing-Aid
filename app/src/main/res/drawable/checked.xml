<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked state -->
    <item android:state_checked="true" android:color="@color/white" />
    <!-- Unchecked state -->
    <item android:state_checked="false" android:color="@color/gy_pewter" />
</selector>
